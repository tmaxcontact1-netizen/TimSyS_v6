# Student profile component contract

Student profile owns extensible student information beyond the core registry and requires the student registry identifier as its parent reference.

It exposes profile capabilities only to authorised administration workflows. It does not own student identity or enrolment lifecycle. Listed parts are internal implementation details.
