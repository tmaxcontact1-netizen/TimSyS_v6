# Academic Commentary contract

Academic Commentary generates advisory drafts from identifiable academic results and optional behaviour and attendance context. It never publishes automatically and never invents personal characteristics. Preferred name comes from Student Registry when available; otherwise neutral student wording is used.

Teachers control final wording. Every edit creates a new version, preserves its source snapshot and lineage, and must be explicitly marked ready before reporting can consume it.
