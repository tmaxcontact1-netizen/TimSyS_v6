# Staff profile component contract

Staff profile owns extensible staff information beyond the core registry and requires the staff registry identifier as its parent reference.

It exposes profile capabilities only to authorised administration workflows. It does not own staff identity or employment lifecycle. Listed parts are internal implementation details.
