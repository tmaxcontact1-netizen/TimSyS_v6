'use strict';const fs=require('fs'),path=require('path'),root=path.join(__dirname,'../../modules/late_entries'),schedule=require(path.join(root,'schedule')),manifest=JSON.parse(fs.readFileSync(path.join(root,'module.json'),'utf8')),sql=fs.readFileSync(path.join(root,'migrations/002_late_entry_schedule_context.sql'),'utf8');
describe('late entry published schedule context',()=>{
 test('resolves calendar coordinates across one and two week rotations',()=>{expect(schedule.cycleCoordinates('2026-08-03T08:10:00.000Z','2026-08-03',1)).toMatchObject({cycle_week:1,day_index:1,time:'08:10'});expect(schedule.cycleCoordinates('2026-08-10T08:10:00.000Z','2026-08-03',2)).toMatchObject({cycle_week:2,day_index:1});expect(schedule.cycleCoordinates('2026-08-17T08:10:00.000Z','2026-08-03',2)).toMatchObject({cycle_week:1,day_index:1})});
 test('retains unresolved and ambiguous context as first-class records',()=>{expect(manifest.schema.tables).toEqual(expect.arrayContaining(['late_entry_schedule_contexts','late_entry_exceptions']));expect(sql).toMatch(/'resolved','ambiguous','unresolved'/);expect(sql).toMatch(/missed_placement_ids_json/);expect(sql).toMatch(/candidate_placement_ids_json/)});
 test('exposes preview without an attendance mutation route',()=>{expect(manifest.routes).toContainEqual(expect.objectContaining({path:'/late-entries/context/preview',method:'POST'}));expect(manifest.events.publishes.some(x=>x.includes('attendance_changed'))).toBe(false)});
});
