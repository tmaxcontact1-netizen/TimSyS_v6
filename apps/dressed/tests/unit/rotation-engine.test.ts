import {describe,expect,test} from "vitest";
import {planRotation} from "../../src/domain/planner/rotation-engine.js";
const policy={completeOutfitDays:30,garmentDays:{top:5,bottom:3,footwear:2,default:3},maximumConsecutiveJacketDays:2,allowGradeC:false};
const candidates=[{id:"a",grade:"A" as const,score:95,garments:[{id:"shirt-a",slotId:"top"},{id:"trouser-a",slotId:"bottom"},{id:"shoe-a",slotId:"footwear"}]},{id:"b",grade:"B" as const,score:82,garments:[{id:"shirt-b",slotId:"top"},{id:"trouser-b",slotId:"bottom"},{id:"shoe-b",slotId:"footwear"}]},{id:"c",grade:"C" as const,score:68,garments:[{id:"shirt-c",slotId:"top"}]}];
describe("rotation planner",()=>{test("deterministically trades grade for healthy rotation",()=>{const input={startDate:"2026-09-06",endDate:"2026-09-08",weekdays:[0,1,2],candidates,policy};const first=planRotation(input),second=planRotation(input);expect(second).toEqual(first);expect(first.map((entry)=>entry.outfitId)).toEqual(["a","b","a"]);expect(first[1]?.explanations[0]).toContain("Grade B")});test("honours fixed/excluded garments and weekday selection",()=>{const result=planRotation({startDate:"2026-09-06",endDate:"2026-09-12",weekdays:[1,3],candidates,policy,fixedGarmentIds:["shirt-b"],excludedGarmentIds:["shoe-a"]});expect(result).toHaveLength(2);expect(result.every((entry)=>entry.outfitId==="b")).toBe(true)});test("does not silently force grade C or an impossible constraint",()=>{expect(()=>planRotation({startDate:"2026-09-06",endDate:"2026-09-06",weekdays:[0],candidates,policy,fixedGarmentIds:["shirt-c"]})).toThrow("no_eligible_saved_outfits")})});
